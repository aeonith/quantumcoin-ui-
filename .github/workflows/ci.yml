# This workflow is disabled - using production-ci.yml instead
# To re-enable, rename this file back to ci.yml

name: Legacy CI (Disabled)

on:
  # Disabled - no triggers
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Legacy workflow disabled
      run: |
        echo "This workflow has been replaced by production-ci.yml"
        echo "The new workflow provides better error handling and reliability"
