# QuantumCoin™ Environment Configuration

# VERCEL DEPLOYMENT VARIABLES (Set in Vercel Dashboard → Settings → Environment Variables)

# Bitcoin Exchange Configuration (REQUIRED)
NEXT_PUBLIC_BTC_ADDRESS=bc1qv7tpdxqvgwutfrhf53nhwgp77j5lv7whnk433y
EXCHANGE_AVAILABLE_FLOAT=250000
# Set to 0 to show "must be mined" message

# QTC Pricing (REQUIRED)
QTC_USD_PRICE=1.00
# Fixed USD price for QTC (used for BTC conversion)

# RevStop Default Setting (REQUIRED)
NEXT_PUBLIC_REVSTOP_DEFAULT_ON=true

# Backend API Configuration (OPTIONAL)
NEXT_PUBLIC_API_BASE=https://your-rust-backend.com
# Leave blank if backend not ready - UI will work with simulated data

# Security & Features (OPTIONAL)
ENABLE_KYC_REQUIREMENT=false
# Set to true to require KYC before exchange access

# Development (LOCAL ONLY)
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# VERCEL DEPLOYMENT INSTRUCTIONS:
# 1. Connect GitHub repository to Vercel
# 2. Set Build Command: npm run build
# 3. Set Output Directory: .next
# 4. Add environment variables above in Vercel dashboard
# 5. Deploy automatically on every push to main branch
