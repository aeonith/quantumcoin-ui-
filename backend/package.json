{
  "name": "quantumcoin-ui",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://aeonith.github.io/quantumcoin-ui-",
  "scripts": {
    "dev": "vite",               // or "start" if using CRA
    "build": "vite build",       // or "react-scripts build"
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist" // change 'dist' to 'build' if using CRA
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^4.5.0",
    "gh-pages": "^5.0.0"
  }
}